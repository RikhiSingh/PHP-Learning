<?php

    // whole number is integer/int
    $radius = 25;
    // this one is float or a double
    $pi = 3.14;

    // basic = -,+, / , *, ** (not ^)

    // echo $pi * $radius**2;

    //B I D M A S
    // brackets Indeces(**) Division multi addition subtraction

    // echo 2 * (4+9) / 3;

    // increment and decrement
    // echo $radius--;
    // echo $radius++;
    // echo $radius;

    // shorthand
    // $age = 20;
    // $age = $age + 10;
    // $age += 10;
    // echo $age;

    // number functions
    // echo floor($pi);
    // echo ceil($pi);
    // inbuilt pi
    // echo pi();

?>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PHP Learning</title>
</head>
<body>
    
</body>
</html>